import SignupComp from "./sign-up"

export default function SignUp() {
    return (
        <main className="flex min-h-screen items-center justify-center bg-[var(--sec-clr)] p-2">
            <SignupComp />
        </main>
    )
}